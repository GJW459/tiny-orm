<mapper namespace="com.gjw.mapper.IUserMapper">

    <select id="queryUserById" parameterType="java.lang.Integer" resultType="com.gjw.mapper.User">
        SELECT id,name,age
        FROM user
        where id = #{id}
    </select>

</mapper>